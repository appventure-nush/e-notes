export interface Settings {
    dark?: boolean;
    drawer?: boolean;
    permanentDrawer?: boolean;
    noTransition?: boolean;
    mini?: boolean;

    showPages?: boolean;
    noHTML?: boolean;
    noLinkify?: boolean;
    lineBreaks?: boolean;

}